{
  "name": "emoji-linter",
  "version": "1.0.0",
  "description": "GitHub Action to validate emoji usage in commits, PRs, and branches",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "lint": "eslint src/"
  },
  "keywords": ["github-action", "emoji", "linter", "validation", "commit-message"],
  "author": "Emily Cogsdill",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}